global:
  scrape_interval: 15s     # pull metrics from targets every 15 seconds

scrape_configs:
  - job_name: "api"
    static_configs:
      - targets: ["api:8000"]   # service name from docker-compose, port 8000
